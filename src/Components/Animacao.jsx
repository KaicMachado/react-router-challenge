import { keyframes } from "styled-components";
export const animeLeft = keyframes`
from {
  transform: translateX(-20px);
  opacity: 0
}
  to {
    transform: initial;
    opacity: initial;
  }
`;
